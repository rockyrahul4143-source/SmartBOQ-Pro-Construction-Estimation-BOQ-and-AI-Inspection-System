# Production requirements for Render.com
# Python 3.11.9 — all packages have pre-built wheels, no compilation needed
# TensorFlow excluded (500MB+ — too large for free tier)

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Database
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# Validation — pydantic v2 with pre-built pydantic-core wheels for Python 3.11
pydantic==2.7.1
pydantic-core==2.18.2
pydantic-settings==2.3.0
email-validator==2.1.1

# Reports
reportlab==4.2.0
openpyxl==3.1.4
xlsxwriter==3.2.0

# DXF parsing
ezdxf==1.3.3

# Image processing — binary wheels only
Pillow==10.4.0
numpy==1.26.4

# Utilities
python-dotenv==1.0.1
httpx==0.27.0
python-dateutil==2.9.0
aiosmtplib==3.0.1
